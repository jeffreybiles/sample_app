%table(summary="Information about the following/followers")
  %tr
    %td
      %h1= @title
      - unless @gods.empty?
        %ul.users
          =render @gods
        =will_paginate

    %td.sidebar.round
      = render 'users/user_display'
      = render 'shared/stats'